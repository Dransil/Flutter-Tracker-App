import 'package:flutter/material.dart';

class SolicitudesAmistadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Aquí implementa la lógica para recuperar y mostrar las solicitudes de amistad
    return Scaffold(
      appBar: AppBar(
        title: Text('Solicitudes de Amistad'),
      ),
      body: Center(
        child: Text('Implementa la lógica aquí'),
      ),
    );
  }
}
